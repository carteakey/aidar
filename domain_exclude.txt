# Domains excluded from HN-discovered scanning.
# These are added here because they consistently fail discovery, are paywalled,
# or produce results that aren't useful for AI writing drift analysis.
#
# Note: platform sites (github.com, youtube.com, reddit.com etc.) are already
# excluded at the HN discovery layer — no need to list them here.
#
# Format: one domain per line, # for comments.
# Add domains from your failed-discovery.log after reviewing.

# --- Consistently fails discovery ---
arxiv.org           # PDFs only, no HTML article sitemap
dl.acm.org          # Paywalled academic, no public sitemap
economist.com       # Paywalled, no public sitemap
idnfinancials.com   # No discoverable sitemap/RSS

# --- Sitemap timeout, no RSS fallback (huge media with Cloudflare) ---
bbc.com
cnbc.com
apnews.com
arstechnica.com     # Timeout consistently; add back if RSS fallback improves
rd.com

# --- Encyclopedia / reference (structured, not prose writing) ---
en.wikipedia.org    # High structural scores but not prose writing drift
wikipedia.org

# --- Paywalled megasites (no public content to scan) ---
bloomberg.com

# --- Not blogs / not prose content ---
etsy.com            # Ecommerce
flydenver.com       # Airport
getlibation.com     # App landing page
startherestudio.gumroad.com  # Gumroad store
actu.epfl.ch        # University news, sitemap timeout

# --- Code / package registries (not prose content) ---
pypi.org            # Package descriptions are too short/structured
